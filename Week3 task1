def get_sum_of_digits(num):
    sum = 0
    digits = []
    while num > 0:
        digits.append(num % 10)
        num //= 10
        print(digits[::-1])
    for i in digits:
        sum += i
    return sum
print(get_sum_of_digits(1233))
